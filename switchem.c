#include <stdio.h>
#include <stdlib.h>
/* Chap 3,  switch statement	*/

int switcheg(int x, int n)
{
    int rv;

    switch (x)
    {
        case 100: // index 0
            ret = ((x * 3) * 4) + x;
            break;
        case 1: 
            w = y*z;
            break;
        case 2:
            w = y/z;
            /* continue */
        case 3:
            w += z;
            break;
        case 5:
        case 6:
            w -= z;
            break;
        default:
            rs = 0;
   }

   return rv;
}
